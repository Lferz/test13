-- Получение модели персонажа
local character = game.Workspace.YourCharacter -- Замените "EblanJPG" на имя вашего персонажа

-- Увеличение модели персонажа
local function increaseSize(character)
    local humanoid = character:FindFirstChild("Humanoid")
    if humanoid then
        humanoid.Scale = Vector3.new(2, 2, 2) -- Увеличьте значения вектора, чтобы увеличить размер модели
    end
end

-- Пример использования:
increaseSize(character)
