local text = "Это ратник блатуха"

-- Создание объекта TextLabel
local textLabel = Instance.new("TextLabel")
textLabel.Text = text
textLabel.Size = UDim2.new(0, 200, 0, 50)
textLabel.Position = UDim2.new(0, 100, 0, 100)
textLabel.Parent = game.Workspace -- или любой другой родительский объект, к которому вы хотите добавить текст

-- Если вы хотите, чтобы текст был редактируемым, используйте объект TextBox вместо TextLabel
-- local textLabel = Instance.new("TextBox")
-- textLabel.Text = text
-- textLabel.Size = UDim2.new(0, 200, 0, 50)
-- textLabel.Position = UDim2.new(0, 100, 0, 100)
-- textLabel.Parent = game.Workspace
